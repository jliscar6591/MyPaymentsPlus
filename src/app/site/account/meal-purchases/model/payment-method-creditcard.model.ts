import { Injectable } from '@angular/core';
@Injectable()

export class CreditCard {
    cardNumber: string;
    cardType: string;
    expiryYear: string;
    expiryMonth: string;
    cardCode: string;
}

